[build]
builder = "DOCKERFILE"
dockerfilePath = "services/texas_bounded_api/Dockerfile"

[deploy]
healthcheckPath = "/readyz"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
