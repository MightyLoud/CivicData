# Texas bounded legislative activation readiness v0.1

Status: `READINESS_CONTRACT_IMPLEMENTED_NOT_ACTIVATED`

This contract defines the final non-mutating gate before the bounded Texas House 49 / Senate 14 representation profile may be proposed for repository activation. It does not activate Texas, add a default catalog route, add a default registry overlay, merge a pull request, or publish unresolved identities.

## Scope

Profile: `tx_legislative_two_office_v0.1`

Consumer route: `state_legislative_representation`

Coverage is exactly:

- Texas House District 49;
- Texas Senate District 14;
- both bindings required atomically;
- representation only;
- omitted data means `NOT_INCLUDED_NOT_ABSENT`;
- `complete_jurisdiction=false` remains true of the bounded view;
- Full Essentials and election scope remain unsupported.

## Production-bounded geography

Legislative geography now supports two explicit scopes:

- `INTERNAL_REVIEW` — the existing non-release preview contract;
- `PRODUCTION_BOUNDED` — a geography-only production declaration that requires explicit geometry governance.

A `PRODUCTION_BOUNDED` group remains `publication_eligible=false` at the geography layer because the polygon resolver supplies geography only, not officeholder publication authority.

The only currently supported production geometry policy is `texas-legislative-geometry-governance/0.1`. Before constructing the resolver, the loader requires the production group to contain exactly the accepted Texas adapter/service/plan tuple:

| Binding | Adapter | District | Plan |
| --- | --- | --- | --- |
| House | `DIST-TX-HOUSE-H2316` | `49` | `PLANH2316` |
| Senate | `DIST-TX-SENATE-S2168` | `14` | `PLANS2168` |

The service URLs must also equal the version-governed Texas service pins. Reusing the Texas policy ID with another service or plan fails closed as `TEXAS_GEOMETRY_GOVERNANCE_BINDING_DRIFT`.

After binding validation, the loader runs the Texas geometry-version preflight before engine construction or address geocoding. Live marker drift fails as `GEOMETRY_VERSION_DRIFT`.

## Activation-readiness envelope

`tools/texas_activation_readiness.py` validates one complete proposed activation without writing it. The proposal must include:

1. the exact bounded production package JSON and SHA-256;
2. the exact production package archive SHA-256 declared by the proposed catalog entry;
3. a current deterministic bounded acceptance receipt for the same package;
4. a proposed `state_legislative_representation` catalog entry using `tx_legislative_two_office_v0.1`;
5. exactly two explicit catalog bindings for House 49 and Senate 14;
6. the exact `PRODUCTION_BOUNDED` Texas legislative geography group generated by `build_texas_production_configuration`;
7. exact-head production deployment evidence;
8. proof that the default package catalog and default Civic GPS extension are still not activated for the Texas route.

The package is validated through the existing bounded production-profile contract. This means public identity disposition must already pass. A provisional Person or provisional-Person warning remains a hard blocker.

## Required deployment evidence

The production deployment evidence is exact-head evidence with:

- `environment=production`;
- `status=PASS`;
- `profile_id=tx_legislative_two_office_v0.1`;
- the exact 40-character candidate head SHA;
- exactly these passing checks:
  - `geometry-governance-preflight`;
  - `package-profile-reconstruction`;
  - `positive-both-bindings`;
  - `outside-slice-negative`;
  - `no-partial-projection`;
  - `public-identity-gate`;
  - `hosted-runtime-route`.

Synthetic tests prove the evidence schema and fail-closed logic; they are not substitutes for actual production deployment evidence.

## Readiness receipt

A successful dry run emits `texas-activation-readiness/0.1` with:

- `status=READY_TO_ACTIVATE`;
- hashes for the package, archive, bounded acceptance receipt, proposed catalog entry, proposed legislative group, and deployment evidence;
- PASS dispositions for production profile, public identity, geometry policy binding, production deployment, and inactive-default checks;
- `activation_authorized=false`;
- `repository_activation=NOT_ACTIVATED`;
- `canonical_writes=0`.

`READY_TO_ACTIVATE` therefore means the exact proposal is structurally ready for a separate explicit activation decision. It is not itself activation authorization.

## Current Day 12 posture

The repository now has a governed activation-readiness path, but the current staged Texas package is not ready for activation because the staged Person identities remain provisional and no exact-head production deployment evidence exists for a resolved package/receipt pair.

The default production catalog and default Civic GPS registry extension remain unchanged with respect to Texas. Any future activation must be a separate reviewed change containing the exact governed package artifact, refreshed acceptance receipt, catalog entry, production legislative overlay, and production deployment evidence.

## Verification

```sh
python tests/test_texas_activation_readiness.py
python tests/civic_gps_texas_activation_runtime_test.py
python tests/test_tx_production_profile.py
python tests/test_public_identity_disposition.py
python -m civic_gps_extensions.texas_geometry_governance
```

The dedicated `Texas activation readiness` workflow runs these controls without changing the default catalog or registry.
